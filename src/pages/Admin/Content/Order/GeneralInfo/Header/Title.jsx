import React from 'react';
import PropTypes from 'prop-types';

Title.propTypes = {
    
};

function Title(props) {
    return (
        <div>
            Title
        </div>
    );
}

export default Title;