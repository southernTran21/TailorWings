import React from 'react';
import PropTypes from 'prop-types';

Note.propTypes = {
    
};

function Note(props) {
    return (
        <div className='admin-order-detail__note'>
            Note
        </div>
    );
}

export default Note;