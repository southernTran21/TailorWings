import React from 'react';
import PropTypes from 'prop-types';

Item.propTypes = {
    
};

function Item(props) {
    return (
        <div>
            Item
        </div>
    );
}

export default Item;