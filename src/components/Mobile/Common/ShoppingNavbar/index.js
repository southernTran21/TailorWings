import React from 'react';
import PropTypes from 'prop-types';

ShoppingNavbar.propTypes = {
    
};

function ShoppingNavbar(props) {
    return (
        <nav>
            ShoppingNavbar
        </nav>
    );
}

export default ShoppingNavbar;