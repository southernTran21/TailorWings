import SizeUpdateModal from 'components/Pages/Cart/SizeUpdateModal';
import React from 'react';

SizeUpdateModalContainer.propTypes = {
    
};

function SizeUpdateModalContainer() {
    return (
        <div className="l-cart__size-update">
            <SizeUpdateModal />
        </div>
    );
}

export default SizeUpdateModalContainer;