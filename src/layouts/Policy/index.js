import React from 'react';
import PropTypes from 'prop-types';

Policy.propTypes = {
    
};

function Policy(props) {
    return (
        <div>
            Policy page
        </div>
    );
}

export default Policy;